global:
  imagePullSecrets:
  - name: private-registry-mil

controller:
  imagePullSecrets: [name: private-registry-mil]

dex:
  imagePullSecrets: [name: private-registry-mil]

redis:
  imagePullSecrets: [name: private-registry-mil]

istio:
  enabled: true

